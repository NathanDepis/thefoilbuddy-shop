{
  "jobs": [
    {
      "functionLocation": "/sync-invoices.js",
      "description": "Reconcile Wix Invoices paid in the last 72h that aren't yet in PendingInvoices",
      "executionConfig": {
        "cronExpression": "0 2 * * *"
      }
    }
  ]
}
